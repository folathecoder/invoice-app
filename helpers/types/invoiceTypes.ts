//TODO: Types => Invoice status
export interface InvoiceStatusTypes {
  paid?: boolean;
  pending?: boolean;
  draft?: boolean;
}

//TODO: Types => All toggle systems
export interface ToggleTypes {
  filterToggle?: boolean;
};
